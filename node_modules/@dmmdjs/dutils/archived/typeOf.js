"use strict";

/**
 * Returns the true type of a value
 * @param {any} v Value
 * @returns {string}
 */
function typeOf(v) {
    return Object.prototype.toString.apply(v).match(/\[object (.*)\]/)[1].toLowerCase();
};

// Exports
module.exports = typeOf;