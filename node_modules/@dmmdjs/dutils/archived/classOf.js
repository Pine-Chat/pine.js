"use strict";

/**
 * Returns the constructor of a value
 * @param {any} v Value
 * @returns {?any}
 */
function classOf(v) {
    if(v === null || v === undefined) return null;
    return Object.getPrototypeOf(v).constructor;
};

// Exports
module.exports = classOf;